# Placeholder for `NuGet.config`

Implemented by T-002 (docs/DEVELOPMENT_GUIDE.md). Create `NuGet.config` and delete this file in the same commit.

Purpose: The single package source.

Required content: <clear /> followed by exactly one add: key third_party, value third_party/nuget (relative path).
