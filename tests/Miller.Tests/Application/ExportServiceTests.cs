// PLACEHOLDER - implemented by T-054 (docs/DEVELOPMENT_GUIDE.md). Replace this header with the tests.
// Namespace: Miller.Tests.Application
// Tests for: src/Miller.Application/Services/ExportService.cs
// Required cases: file written in a temp directory; extension enforced; unknown post-processor id
//     throws before creating the file; UTF-8 without BOM
// Rules: deterministic, no network, temp directories only, every strategy test calls GougeChecker.Verify.
