// PLACEHOLDER - implemented by T-058 (docs/DEVELOPMENT_GUIDE.md). Replace this header with the tests.
// Namespace: Miller.Tests.App
// Tests for: src/Miller.App/Styles/Colors.axaml
// Required cases: scan src/**/*.axaml and src/**/*.cs for #RRGGBB(AA), Color.Parse(,
//     Color.FromRgb(; only Styles/Colors.axaml may contain them
// Rules: deterministic, no network, temp directories only, every strategy test calls GougeChecker.Verify.
