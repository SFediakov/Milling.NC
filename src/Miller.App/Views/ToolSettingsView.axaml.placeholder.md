# Placeholder for `ToolSettingsView.axaml`

Implemented by T-066 (docs/DEVELOPMENT_GUIDE.md). Create `src/Miller.App/Views/ToolSettingsView.axaml` and delete this file in the same commit.

Purpose: Cutter diameter, cutter length, head diameter, tip type, name; validation messages; schematic of the tool with the wider head.

Required content: UserControl or Window bound to ToolSettingsViewViewModel; every color through DynamicResource from Colors.axaml.
