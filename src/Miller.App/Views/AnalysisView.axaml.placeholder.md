# Placeholder for `AnalysisView.axaml`

Implemented by T-099 (docs/DEVELOPMENT_GUIDE.md). Create `src/Miller.App/Views/AnalysisView.axaml` and delete this file in the same commit.

Purpose: Analyze button, legend of the six categories, counts, areas, volumes; toggles stock view, final model view, uncuttable overlay.

Required content: UserControl or Window bound to AnalysisViewViewModel; every color through DynamicResource from Colors.axaml.
