# Placeholder for `MainWindow.axaml`

Implemented by T-063 (docs/DEVELOPMENT_GUIDE.md). Create `src/Miller.App/Views/MainWindow.axaml` and delete this file in the same commit.

Purpose: Main window: menu (top), settings TabControl (left), Viewport3DControl (center), status bar with progress and cancel (bottom). Window size restored from settings.

Required content: UserControl or Window bound to MainWindowViewModel; every color through DynamicResource from Colors.axaml.
