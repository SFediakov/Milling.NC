# Placeholder for `StockSettingsView.axaml`

Implemented by T-067 (docs/DEVELOPMENT_GUIDE.md). Create `src/Miller.App/Views/StockSettingsView.axaml` and delete this file in the same commit.

Purpose: Shape (Box, Cylinder) switching the dimension fields, placement, margin, Fit to model button.

Required content: UserControl or Window bound to StockSettingsViewViewModel; every color through DynamicResource from Colors.axaml.
