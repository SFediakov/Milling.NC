# Placeholder for `AxisSettingsView.axaml`

Implemented by T-068 (docs/DEVELOPMENT_GUIDE.md). Create `src/Miller.App/Views/AxisSettingsView.axaml` and delete this file in the same commit.

Purpose: Axis mapping combos, direction check boxes, rotation fields, origin mode with custom offset, read-only machine-space bounds.

Required content: UserControl or Window bound to AxisSettingsViewViewModel; every color through DynamicResource from Colors.axaml.
