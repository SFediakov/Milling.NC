// PLACEHOLDER - implemented by T-011 (docs/DEVELOPMENT_GUIDE.md). Replace this header with the implementation.
// Namespace: Miller.Core.Geometry
// Purpose: Axis-aligned box in machine or model coordinates.
// Public interface (names only): readonly struct BoundingBox { Vector3 Min; Vector3 Max; Vector3
//     Size; Vector3 Center; bool IsEmpty; static BoundingBox Empty; BoundingBox Union(BoundingBox);
//     BoundingBox Include(Vector3); bool Contains(Vector3); bool Contains(BoundingBox) }
// Depends on: none
// Must not depend on: Avalonia, System.IO file dialogs, threads, timers
