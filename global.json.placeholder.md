# Placeholder for `global.json`

Implemented by T-001 (docs/DEVELOPMENT_GUIDE.md). Create `global.json` and delete this file in the same commit.

Purpose: Pins the .NET SDK for every developer and the Docker build.

Required content: {"sdk": {"version": "10.0.100", "rollForward": "latestFeature"}}
