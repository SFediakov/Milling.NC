#!/usr/bin/env bash
# PLACEHOLDER - implemented by T-009 (docs/DEVELOPMENT_GUIDE.md). Replace this header with the script.
# Purpose: Linux start file, copied next to the published Miller binary by build.sh.
# Steps:
#   1. cd "$(dirname "$(readlink -f "$0")")"
#   2. exec ./Miller "$@"
#   3. LF line endings only (.gitattributes enforces it)
