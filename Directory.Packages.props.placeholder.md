# Placeholder for `Directory.Packages.props`

Implemented by T-002 (docs/DEVELOPMENT_GUIDE.md). Create `Directory.Packages.props` and delete this file in the same commit.

Purpose: Central package management with every pinned version from docs/ARCHITECTURE.md 2.2.

Required content: ManagePackageVersionsCentrally true; one PackageVersion element per package; no floating versions.
